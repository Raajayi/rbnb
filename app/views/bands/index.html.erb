<h1>Bands#index</h1>
<p>Find me in app/views/bands/index.html.erb</p>

<p style="color: green"><%= notice %></p>

<div id="restaurants" class="row mb-2">
  <% @bands.each do |band| %>
    <%= render band %>
  <% end %>
</div>

<%= link_to "New restaurant", new_band_path %>
